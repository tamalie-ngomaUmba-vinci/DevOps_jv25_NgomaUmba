const express = require('express');
const router = express.Router();

// router.post('passwordCheker', function (req, res, next ) {
  console.log('test pour les mots de passes');
}

module.exports = router;
